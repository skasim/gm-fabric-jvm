machine:
    java:
        version: oraclejdk8

dependencies:
    override:
        - mvn --projects archetype,core dependency:go-offline

test:
    override:
        - mvn verify

deployment:
    override:
        - if [ -z "${CI_PULL_REQUEST}" ]; then mvn deploy -P deploy -s settings.xml; fi
